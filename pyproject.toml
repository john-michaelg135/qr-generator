[project]
name = "qr-generator"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<0.116",
    "qrcode[pil]>=7.4,<8",
    "Pillow>=10.4,<11",
]

[tool.vercel]
entrypoint = "api.generate:app"
